// Chiedi all’utente la sua email per poi controllare che sia nella lista di chi può accedere.
// Stampa un messaggio SOLO SE l'utente è in lista.

// chiedi la mail all'utente
const mail = ("davide@prova.it");
console.log (mail);
const userMail = prompt ("scrivi qui la mail");
console.log (userMail)

// fai comparire un messaggio se presente
if (userMail == mail) {
    alert ("you inn")
}